To build solution use build.bat script in vs command prompt. 
But you need also specify at least two parameters: boost include directory and boost library directory.
Program tested via boost 1.63 and msbuild 14.0.25420.1 version.